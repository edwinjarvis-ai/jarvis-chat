# OpenClaw Gateway WebSocket URL (use wss:// for secure connections)
NEXT_PUBLIC_GATEWAY_URL=wss://your-gateway-tunnel.trycloudflare.com

# Gateway authentication token (from ~/.openclaw/openclaw.json gateway.auth.token)
NEXT_PUBLIC_GATEWAY_TOKEN=your-gateway-token
